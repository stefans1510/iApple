export class AboutUs {
    title: string;
    text: string;
    contact: string;

    constructor(title: string, text: string, contact: string) {
        this.title = title;
        this.text = text;
        this.contact = contact;
    }
}