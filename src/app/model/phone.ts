export class Phone {
    id: number;
    model: string;
    price: number;

    constructor(id: number, model: string, price: number) {
        this.id = id;
        this.model = model;
        this.price = price;
    }
}